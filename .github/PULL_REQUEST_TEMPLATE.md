## What this changes

<!-- What was wrong before, and what is different now. If it fixes an issue,
     say which one. -->

## How you know it works

<!-- What you ran. If the change touches anything that talks to tshark, say
     which tshark version you tested against - that is where this program
     breaks. -->

- [ ] `go test ./...` passes with `tshark` on PATH
- [ ] `gofmt -l .` prints nothing
- [ ] `go vet -composites=false ./...` is clean

## Anything a reviewer should know

<!-- Trade-offs, things you were unsure about, parts you would like a second
     opinion on. "Nothing" is a fine answer. -->
